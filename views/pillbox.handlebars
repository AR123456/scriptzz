<div class="jumbotron">
  <h1 class="display-3">Medication Schedule</h1>
  <p class="lead"></p>
  <hr class="my-4">
</div>

<!-- Top Row -->
<div class="container-fluid">
  <div class="row">
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Morning</h4></div>
      <div class="card-body">
        <p class="card-text">Medication: {{#each scriptz}}{{#if active}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/if}}{{/each}} </p>
        {{!-- the taken button needs to decrement pill count on medication page text change from mark taken to taken after click  --}}
          {{!-- <p>  <button type ="button" class ="btn btn-primary">Mark Taken </button></p> --}}
      </div>  
    </div>

    </div>
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Lunch Time</h4></div>
      <div class="card-body">
        <p class="card-text">Medication: {{#each scriptzLunch}}{{#if active}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/if}}{{/each}}</p>
        <p> <button type ="button" class ="btn btn-primary">Mark Taken </button></p>
      </div>  
    </div>      

    </div>
  </div>
</div>
<!-- Bottom Row -->
<div class="container-fluid">
  <div class="row">
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Dinner Time</h4></div>
      <div class="card-body">
        <p class="card-text">Medication{{#each scriptzDinner}}{{#if active}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/if}}{{/each}}</p>
      <p> <button type ="button" class ="btn btn-primary">Mark Taken </button></p>
      </div>  
    </div>

    </div>
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Before Bed</h4></div>
      <div class="card-body">
        <p class="card-text">Medication {{#each scriptzBed}}{{#if active}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/if}}{{/each}}</p>
           <p><button type ="button" class ="btn btn-primary">Mark Taken </button></p>
      </div>  
    </div>      
    </div>
  </div>
</div>

<div id="mymodal" class="modal bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      ...
    </div>
  </div>
</div>